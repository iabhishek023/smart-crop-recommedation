import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Default styling or your own styling
import App from './App';  // Make sure the path is correct

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


